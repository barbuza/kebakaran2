{
  "name": "kebakaran",
  "version": "0.5.0",
  "description": "high level utilities for firebase interaction",
  "main": "dist/lib/index.js",
  "scripts": {
    "test": "rimraf dist && tslint lib/*.ts tests/**/*.ts && tsc && tap dist/tests/*.js",
    "cover": "nyc tape dist/tests/*.js",
    "coveralls": "nyc tape dist/tests/*.js && nyc report --reporter=text-lcov | coveralls"
  },
  "author": {
    "name": "Victor Kotseruba",
    "url": "https://github.com/barbuza"
  },
  "license": "ISC",
  "peerDependencies": {
    "immutable": "^3.8.1"
  },
  "devDependencies": {
    "coveralls": "^2.11.9",
    "immutable": "^3.8.1",
    "nyc": "^6.6.1",
    "rimraf": "^2.5.3",
    "tap": "^6.1.1",
    "tape": "^4.6.0",
    "tslint": "^3.13.0",
    "typescript": "^2.0.0",
    "typings": "^1.3.1"
  }
}
